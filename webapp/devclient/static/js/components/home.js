/* components/home.js */
var Home = Vue.extend({
	template: document.querySelector('template[id=tmpl-home]'),

	title: 'Home',

	computed: Vuex.mapGetters([
	]),

	methods: Vuex.mapActions([
	]),
});
